<!DOCTYPE html>
<html lang="en">
<%- include('partials/htmlHead', {title: title}) %>

    <body class="d-flex align-items-center py-4 bg-body-tertiary">
        <main class="w-100 m-auto" style="max-width: 330px; padding: 1rem;">
            <form action="/login" method="post">
                <div class="d-flex flex-column align-items-center justify-content-center">
                    <img src="/steering-wheel.png" height="110" width="110" alt="">
                    <h1 class="fw-medium">Sure<span class="fw-bold">Drive</span></h1>
                </div>
                <h2>Login</h2>
                <% if (locals.error.length!==0) {%>
                    <div class="alert alert-danger" role="alert">
                        <p>
                            <%= error %>
                        </p>
                    </div>
                    <% } %>
                        <div class="form-floating">
                            <input class="form-control" type="text" name="username" id="floatingUsername"
                                placeholder="username"
                                style="margin-bottom: -1px; border-bottom-right-radius: 0; border-bottom-left-radius: 0;"
                                required>
                            <label for="floatingUsername">Username</label>
                        </div>
                        <div class="form-floating">
                            <input class="form-control" type="password" name="password" id="floatingPassword"
                                placeholder="password"
                                style="margin-bottom: 10px; border-top-left-radius: 0; border-top-right-radius: 0;"
                                required>
                            <label for="floatingPassword">Password</label>
                        </div>
                        <button class="btn btn-primary w-100 py-2 my-3" type="submit">Login</button>
                        <small>Don't have an account? <a href="/sign-up">Sign up here!</a></small>
            </form>

        </main>
    </body>

</html>